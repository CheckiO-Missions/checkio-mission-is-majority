"use strict";

function isMajority(a) {
    // your code here
    return undefined;
}

var assert = require('assert');

if (!global.is_checking) {
    console.log('Example:');
    console.log(isMajority([true, true, false, true, false]));

    // These "asserts" are used for self-checking
    assert.equal(isMajority([true, true, false, true, false]), true);
    assert.equal(isMajority([true, true, false]), true);
    assert.equal(isMajority([true, true, false, false]), false);
    assert.equal(isMajority([true, true, false, false, false]), false);
    assert.equal(isMajority([false]), false);
    assert.equal(isMajority([true]), true);
    assert.equal(isMajority([]), false);

    console.log("Coding complete? Click 'Check' to earn cool rewards!");
}
    